const pagination = document.querySelector('#pagination');

if (pagination) {
    pagination.addEventListener('click',async(e)=>{
        pag = e.target;

        if(pag.classList.contains('page')) {
            
        }
    })
}